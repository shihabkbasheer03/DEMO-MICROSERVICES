apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:  # changed 'label' to 'labels'
    name: result-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80  # changed 'targetport' to 'targetPort'
      nodePort: 30005  # changed 'NodePort' to 'nodePort'
  selector:
    app: demo-voting-app  # typically selectors use labels, simplified here
