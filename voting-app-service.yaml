apiVersion: v1  
kind: Service 
metadata:
  name: voting-service
  label:
    name: voting-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetport: 80
      NodePort: 30004
  selector:
    name: voting-app-pod
    app: demo-voting-app
